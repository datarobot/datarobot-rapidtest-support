{{- range .Values.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.name }}
type: Opaque
stringData:
  value: {{ required "A secret value must be provided" .value | quote }}

{{ end }}
